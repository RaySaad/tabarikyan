<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">

    <!-- مفتاح تفعيل "الحذف النهائي الإداري" - مُغلَق افتراضياً (False).
         noupdate="1" حتى لا تُعاد قيمته لـ False تلقائياً عند كل ترقية
         للموديول لو كان المستخدم قد فعّله فعلاً - يُعدَّل يدوياً فقط من
         الإعدادات > التقنية > معاملات النظام. -->
    <record id="config_parameter_admin_delete_enabled" model="ir.config_parameter">
        <field name="key">bank_settlement.admin_delete_enabled</field>
        <field name="value">False</field>
    </record>

</odoo>
